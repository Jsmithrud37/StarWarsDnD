/**
 * Sorting attribute
 */
export enum SortBy {
	NameAscending = 'Name (A-Z)',
	NameDescending = 'Name (Z-A)',
	PCsFirst = 'PCs First',
	NPCsFirst = 'NPCs First',
}

export enum CharacterKindFilter {
	All = 'All',
	PCs = 'Player Characters',
	NPCs = 'NPCs',
}
